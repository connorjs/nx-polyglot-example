{
	"compilerOptions": {
		"baseUrl": ".",
		"emitDeclarationOnly": false, // Because this is an app
		"noEmit": true, // Because this is an app
		"outDir": "./build/tsc"
	},
	"extends": "../../tsconfig.options.json",
	"include": ["src", "vite.config.ts"],
	"references": [{ "path": "../../libs/hello-js" }]
}
