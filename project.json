{
	"$schema": "https://raw.githubusercontent.com/nrwl/nx/master/packages/nx/schemas/project-schema.json",
	"implicitDependencies": [
		"hello-cs",
		"hello-js",
		"weather-api",
		"weather-api-spec"
	],
	"name": "root",
	"targets": {
		"build": {
			"dependsOn": ["^build"]
		},
		"ci": {
			"dependsOn": ["build", "format", "lint", "test"],
			"executor": "nx:noop"
		},
		"clean": {
			"command": "nx run-many --target clean --exclude root"
		},
		"format": {
			"command": "prettier '*.{cjs,js,json,md}' --ignore-path .gitignore --list-different",
			"dependsOn": ["^format"]
		},
		"format:fix": {
			"executor": "nx:run-commands",
			"options": {
				"commands": [
					"dotnet csharpier .",
					"prettier '**/*.{cjs,css,cts,graphql,html,js,json,jsonc,jsx,md,pcss,scss,ts,tsp,tsx}' --ignore-path .gitignore --write"
				]
			}
		},
		"lint": {
			"command": "eslint -f pretty '*.{cjs,js,json,md}'",
			"dependsOn": ["^lint"]
		},
		"test": {
			"dependsOn": ["^test"]
		}
	}
}
